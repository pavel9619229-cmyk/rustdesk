# Этап 1.8 — восстановление реального подключения

Дата: 2026-08-29.
Устройства: `desktop` (оператор) → `local-server` (клиент).

## Причина исходного отказа

HAPP направлял `77.222.38.70` через `happ-tun` (`172.18.0.2`).
TCP-сокеты открывались, но HTTPS `:8443` завершался тайм-аутом.
На `desktop` добавлен постоянный маршрут `77.222.38.70/32`
через Ethernet, шлюз `192.168.1.1`, interface index 3.
После исправления `:8443` отвечает, а rendezvous использует физический адрес `192.168.1.28`.

## Серверное право

Оператор: `294098875`.
Команда `python3 masha_auth.py admin allow 294098875` на VPS вернула `ok`.
Первичный отказ `operator_expired` после этого устранён.

## Реальный сеанс

Цель: `114983435` (`local-server`).
Новый сеанс запущен 2026-08-29 19:39 MSK.
Rendezvous: `77.222.38.70:21116`; relay: `77.222.38.70:21117`.
Authorize: успешное HTTPS-соединение с `/v1/session/authorize`.
Получен `PeerInfo` клиента, создан H264 decoder, видео установлено на 30 FPS.
Итог: реальное подключение `desktop → local-server` успешно.
